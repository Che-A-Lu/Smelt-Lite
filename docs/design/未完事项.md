# 炼知 / Smelt — 未完事项

> 最后更新：2026-07-17

## 已完成（全部 P0-P3）

| # | 项 |
|---|-----|
| 1 | 卡片快照预览（图片/文本/CSV 缩略图 + 懒加载 + LRU 缓存） |
| 2 | 上下文卡片内容自动注入 AI system prompt |
| 3 | 上下文编辑浮层（注释 + 全内容/仅标题 + 恢复原始） |
| 4 | 思考链三档发送（关闭/仅最近/全部） |
| 5 | 对话片段提取为卡片 |
| 6 | 多选/框选（Shift+拖框、多卡拖拽、批量删除） |
| 7 | 脚本沙箱执行（Worker 里跑脚本，`script_run` 真实执行） |
| 8 | 打包导出（右键导出原文件 + 打包为 .card + 编辑面板 + 隐私扫描 + 加密 + 签名） |
| 9 | 每文件独立 hash + edits.json 转手历史 |
| 10 | 联系人信任列表（指纹 + 重复接触计数 + 加为信任） |
| 11 | 密钥导出/导入（跨浏览器迁移身份） |
| 12 | 工作台模板导出 |
| 13 | 主题卡应用（拖 JSON 卡到卡座 → 覆盖全局交互参数） |
| 14 | 小地图 |
| 15 | 右键菜单组件化（ContextMenu 统一组件） |
| 16 | 多工作台并行 + 会话持久化（2 秒防抖自动保存） |
| 17 | 工作台布局重构（左图标列 + 抽屉 + 导航条） |
| 18 | 适配器层（OpenAI/Anthropic/Gemini 原生协议） |
| 19 | 工具系统（6 内置 tool + 声明三段式 + 执行器） |
| 20 | 自动/手动模式 + 确认 UI |
| 21 | 模型选择器 |
| 22 | 模式卡（管线/团队/编排 三种执行器 + 凹槽 UI） |
| 23 | 工具自动推荐（关键词匹配补充 tools） |
| 24 | Space 快速预览 + Ctrl+S 手动保存 + 快捷键帮助面板 |
| 25 | 中英双语全量覆盖 |
| 26 | 交互参数全部可覆盖（InteractionConfig） |
| 27 | CSS 动画全集（cardBreathe/blinkCursor/spin/panelIn/fadeIn） |
| 28 | 存储持久化（persist API + 状态条 + 配额监控） |

## 未完（延后或待定）

| # | 项 | 说明 |
|---|-----|------|
| 1 | **独立打包工具** | 用户不进空间就能创建 .card 的独立网页。空间内打包已做完，独立工具等用户量起来再做 |
