# 普通文件拖入 · 施工指令

## 现状

- `.card` 文件拖入正常（走 ImportDialog）
- 普通文件（CSV、Excel、图片、md、txt、pdf 等）拖入 `// TODO`
- `createSphereFromFile(file, position)` 已在 `platform/storage.ts` 第 253 行，返回 `{ sphere, card }`

## 改一个文件：`ui/canvas/Canvas.tsx`

### 1. 补充 import

第 4 行已有：

```typescript
import { saveWorkspaceCamera, updateCard, moveCardToPack, activateCardInDock, deleteCard } from "../../platform/storage";
```

末尾加上 `createSphereFromFile`：

```typescript
import { saveWorkspaceCamera, updateCard, moveCardToPack, activateCardInDock, deleteCard, createSphereFromFile } from "../../platform/storage";
```

### 2. 填充 onDrop

找到 `onDrop`（约第 273 行），当前：

```typescript
const onDrop = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    setDropActive(false);
    const file = e.dataTransfer.files[0];
    if (file && (file.name.endsWith(".card") || file.name.endsWith(".zip"))) {
      onFileDrop?.(file);
    }
    // TODO: 普通文件创建卡片
  }, [onFileDrop]);
```

改为：

```typescript
const onDrop = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    setDropActive(false);

    const files = Array.from(e.dataTransfer.files);
    if (files.length === 0) return;

    // .card / .zip → 导入流程
    const cardFiles = files.filter((f) => f.name.endsWith(".card") || f.name.endsWith(".zip"));
    const regularFiles = files.filter((f) => !f.name.endsWith(".card") && !f.name.endsWith(".zip"));

    if (cardFiles.length > 0) {
      onFileDrop?.(cardFiles[0]);
      if (regularFiles.length === 0) return;
    }

    // 普通文件 → 创建球 + 卡片
    const v = view.current;
    const baseX = (-v.x + window.innerWidth / 2) / v.zoom;
    const baseY = (-v.y + window.innerHeight / 2) / v.zoom;

    for (const file of regularFiles) {
      createSphereFromFile(file, {
        x: baseX + (Math.random() - 0.5) * 200,
        y: baseY + (Math.random() - 0.5) * 100,
      }).then(() => onRefresh()).catch((err) => console.warn("File import failed", err));
    }
  }, [onFileDrop, onRefresh]);
```

### 3. 原理

- 多文件支持：一批拖入的文件，`.card` 走导入，普通文件逐个创建球+卡片
- 位置：卡片出现在当前画布视口中央附近（加随机偏移避免重叠）
- `createSphereFromFile` 内部会调 `createCard` + `bindCardToSphere` → 写入 OPFS
- 调 `onRefresh()` → App 层重新读 index → 画布重渲染 → 新卡片以创建动画出现

## 验收

1. 从桌面拖一个 CSV 文件到空间 → 卡片出现在画布上，名称=文件名（去扩展名），类型角标=CSV（绿色）
2. 同时拖 3 个文件 → 3 张卡片出现
3. 同时拖 1 个 .card + 2 个普通文件 → .card 弹出导入流程，2 张卡片出现
4. `npx tsc --noEmit` 零错误
