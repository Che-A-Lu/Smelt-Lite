# 打包导出 · 完整施工指令

## 现状

- `features/export/index.ts`：`buildCardPackage`、`downloadBlob`、`scanContent`、AES-256-GCM 加密/解密、密码强度检测——已全部实现
- `features/identity/index.ts`：ECDSA P-256 密钥生成/存储、`signContent`/`verifySignature`/`buildProvenance`/`verifyChain`——已全部实现
- `ui/panels/ExportPanel.tsx`：**空壳**，只显示"即将实现"
- 右键菜单没有导出入口

## 目标

| 操作 | 入口 | 行为 |
|------|------|------|
| 导出原文件 | 右键单张卡 → "导出文件" | 直接下载，不弹面板 |
| 单张打包 | 右键单张卡 → "打包为 .card" | 弹编辑面板 → 下载 |
| 多张打包 | 右键选中卡 → "打包选中 N 张" | 弹编辑面板 → 下载 |
| 每文件 hash | 打包时自动写入 manifest | 导入时定位修改 |
| edits.json | 打包时自动生成 | 导入时展示转手历史 |
| 密钥导出/导入 | 设置面板 | 跨浏览器迁移身份 |
| 信任列表 | 导入面板 | 重复接触建立信任 |

---

## 一、导出原文件——右键直接下载

### CardView 右键加菜单项

在 `onContextMenu` 中加：

```typescript
menu.innerHTML = `
  <div data-action="detail" style="padding:4px 12px;cursor:pointer;">View detail</div>
  <div data-action="export-file" style="padding:4px 12px;cursor:pointer;">导出文件</div>
  <div style="border-top:1px solid #e5e7eb;margin:2px 0;"></div>
  <div data-action="export-card" style="padding:4px 12px;cursor:pointer;">打包为 .card</div>
`;

// 导出文件按钮：
menu.querySelector('[data-action="export-file"]')!.addEventListener("click", async () => {
  if (!card.sphereId) return;
  const files = await readSphereFiles(card.sphereId);
  const real = files.filter((f) => f.name !== "_snapshot.png");
  if (real.length === 0) return;
  const blob = new Blob([real[0].content], { type: "application/octet-stream" });
  downloadBlob(blob, real[0].name);
  menu.remove();
});

// 打包按钮 → 调 onExportCard?.(card.id)
```

### CardView 新增 prop

```typescript
onExportCard?: (cardId: string) => void;
```

---

## 二、Canvas 整合多选导出

### Canvas 加状态

```typescript
const [exportingCardIds, setExportingCardIds] = useState<Set<string> | null>(null);
```

### 传 prop 给 CardView

```typescript
onExportCard={(cardId) => setExportingCardIds(new Set([cardId]))}
```

### 多选右键

当 `selectedIds.size > 1` 时，Canvas 的右键处理检测到点击的卡在选中集中 → 菜单加"打包选中 N 张"。

### Canvas 渲染 ExportPanel

```typescript
{exportingCardIds && (
  <ExportPanel
    cardIds={exportingCardIds}
    allCards={Object.values(index.cards)}
    onClose={() => setExportingCardIds(null)}
  />
)}
```

---

## 三、重写 ExportPanel（打包面板）

### 状态

```typescript
export function ExportPanel({ cardIds, allCards, onClose }: {
  cardIds: Set<string>;
  allCards: CardEntry[];
  onClose: () => void;
}) {
  const selectedCards = allCards.filter((c) => cardIds.has(c.id));
  const [manifest, setManifest] = useState({
    label: selectedCards[0]?.label ?? "",
    author: "Anonymous",
    version: "1.0.0",
    tags: "",
    description: "",
  });
  const [fileList, setFileList] = useState<{
    cardLabel: string; fileName: string; size: number; content: string;
  }[]>([]);
  const [includeFiles, setIncludeFiles] = useState<Set<string>>(new Set());
  const [password, setPassword] = useState("");
  const [useSignature, setUseSignature] = useState(true);
  const [useEncryption, setUseEncryption] = useState(false);
  const [readmeContent, setReadmeContent] = useState("");
  const [showReadme, setShowReadme] = useState(false);
  const [editNote, setEditNote] = useState("");  // 修改说明
  const [exporting, setExporting] = useState(false);
```

### 加载文件清单（useEffect）

遍历选中卡片 → `readSphereFiles` → 过滤 `_snapshot.png` → 填入 fileList。

### 生成 README

```typescript
const generateReadme = () => {
  const lines = [
    `# ${manifest.label}`,
    "",
    `作者：${manifest.author}  ·  版本：${manifest.version}`,
    manifest.tags ? `标签：${manifest.tags}` : "",
    manifest.description || "",
    "",
    "## 文件",
    ...fileList.filter((f) => includeFiles.has(f.fileName))
      .map((f) => `- ${f.fileName}（${f.cardLabel}）`),
    "",
    "拖入 Smelt 空间即可查看和继续编辑。",
  ].filter(Boolean);
  setReadmeContent(lines.join("\n"));
  setShowReadme(true);
};
```

### 隐私扫描

对每个勾选文件调 `scanContent` → 汇总结果 → 底部展示警告列表。

### 打包函数

```typescript
const doExport = async () => {
  setExporting(true);
  
  // 构建 edits log
  const editsLog: EditsLog = {
    originalAuthor: manifest.author,
    originalHash: "", // 首次导出为空
    chain: editNote.trim() ? [{
      editor: manifest.author,
      timestamp: Date.now(),
      note: editNote,
      changes: { added: [], removed: [], modified: [] },
    }] : [],
  };

  // README 作为额外文件
  const extraFiles: ExtraFile[] = [];
  if (readmeContent.trim()) {
    extraFiles.push({
      file: new File([new Blob([readmeContent])], "README.md"),
      name: "README.md",
    });
  }

  for (const card of selectedCards) {
    if (!card.sphereId) continue;
    const filter = new Set(
      fileList.filter((f) => f.cardLabel === card.label && includeFiles.has(f.fileName))
        .map((f) => f.fileName)
    );
    const blob = await buildCardPackage({
      card,
      sphere: /* 从 index 读 */,
      sphereFileFilter: filter,
      extraFiles: card === selectedCards[0] ? extraFiles : [],
      shell: BUILTIN_SHELLS[0],
      manifest: {
        id: nanoid(),
        label: manifest.label,
        version: manifest.version,
        author: manifest.author,
        description: manifest.description,
        tags: manifest.tags.split(",").map((t) => t.trim()).filter(Boolean),
        requires: [],
        createdAt: Date.now(),
        exportedAt: Date.now(),
      },
      password: useEncryption ? password : null,
      authorName: manifest.author,
      previousEditsLog: editsLog,
      editNote,
    });
    downloadBlob(blob, `${manifest.label || "export"}.card`);
  }
  onClose();
};
```

### UI 布局

```
┌──────────────────────────────────────────┐
│  打包为 .card                       [×]  │
│                                          │
│  卡片名称：[年度分析报告_______________]  │
│  作者：    [Dalu______________________]  │
│  版本：    [1.0.0_____________________]  │
│  标签：    [分析 报告 Q3______________]  │
│                                          │
│  ── 文件清单 ──────────────────────────  │
│  ☑ data.xlsx         45KB  (销售数据)    │
│  ☑ report.md         12KB  (分析报告)    │
│  ☐ notes.txt          3KB               │
│  [+ 生成 README.md]                      │
│                                          │
│  修改说明（可选）：                       │
│  ┌──────────────────────────────────┐   │
│  │ 更新了Q3数据，删除过时模板        │   │
│  └──────────────────────────────────┘   │
│                                          │
│  ⚠ 隐私扫描：发现 1 个问题              │
│  · data.xlsx 第15行：疑似 API Key       │
│                                          │
│  ── 安全 ────────────────────────────   │
│  ☑ 签名（Dalu · ECDSA P-256）           │
│  ☐ 加密  密码：[________] 强度：弱       │
│                                          │
│  [取消]                    [打包下载]    │
└──────────────────────────────────────────┘
```

---

## 四、每文件 hash + edits.json

### 4.1 新增 `hashSingleFile`（`features/identity/index.ts`）

```typescript
export async function hashSingleFile(name: string, content: string): Promise<string> {
  const hash = await crypto.subtle.digest("SHA-256",
    new TextEncoder().encode(`${name}:${content}`));
  return "sha256:" + Array.from(new Uint8Array(hash))
    .map((b) => b.toString(16).padStart(2, "0")).join("");
}
```

### 4.2 改 `buildCardPackage`

- `manifest.json` 加 `files` 字段：`{ "data.csv": "sha256:...", "report.md": "sha256:..." }`
- 接收 `previousEditsLog` 参数 → 追加新 EditRecord → 写入 `edits.json`

### 4.3 新增类型（`features/export/index.ts`）

```typescript
export interface EditRecord {
  editor: string; timestamp: number; note: string;
  changes: { added: string[]; removed: string[]; modified: { file: string; oldHash: string; newHash: string }[] };
}
export interface EditsLog {
  originalAuthor: string; originalHash: string; chain: EditRecord[];
}
```

---

## 五、密钥导出/导入

### 5.1 新增函数（`features/identity/index.ts`）

```typescript
// 导出 → 下载 JSON 文件
export async function exportIdentityKey(): Promise<void> {
  const kp = await getOrCreateKeyPair();
  const privJWK = await crypto.subtle.exportKey("jwk", kp.privateKey);
  const pubJWK = await crypto.subtle.exportKey("jwk", kp.publicKey);
  const blob = new Blob([JSON.stringify({ type: "smelt-identity-key", version: 1, privateKey: privJWK, publicKey: pubJWK }, null, 2)], { type: "application/json" });
  downloadBlob(blob, "smelt-identity-key.json");
}

// 导入 → 覆盖 OPFS 中的密钥
export async function importIdentityKey(file: File): Promise<{ fingerprint: string; success: boolean }> {
  try {
    const keyFile = JSON.parse(await file.text());
    if (keyFile.type !== "smelt-identity-key") throw new Error("Invalid");
    keyPair = {
      privateKey: await crypto.subtle.importKey("jwk", keyFile.privateKey, { name: "ECDSA", namedCurve: "P-256" }, false, ["sign"]),
      publicKey: await crypto.subtle.importKey("jwk", keyFile.publicKey, { name: "ECDSA", namedCurve: "P-256" }, true, ["verify"]),
    };
    // 写入 OPFS
    // ...
    const fingerprint = await getPublicKeyFingerprint();
    return { fingerprint, success: true };
  } catch { return { fingerprint: "", success: false }; }
}
```

### 5.2 设置面板加身份区域

```
身份

当前指纹：a3f2 8d71 4e0b 9c6f

[导出身份密钥]  [导入身份密钥]
```

导入用 `<input type="file">` → 成功显示新指纹 + Toast。

---

## 六、导入面板增强

### 6.1 每文件 hash 校验

安全检查步骤（step 2）加：

```typescript
// manifest.files 里每个文件的 hash 与实际文件对比
for (const [name, expectedHash] of Object.entries(manifest.files ?? {})) {
  const actual = contentFiles.find((f) => f.name === name);
  if (!actual) warnings.push({ message: `文件缺失：${name}` });
  else {
    const actualHash = await hashSingleFile(name, actual.data);
    if (actualHash !== expectedHash) warnings.push({ message: `文件已修改：${name}` });
  }
}
```

### 6.2 edits.json 展示

签名步骤后展示转手历史（可折叠）：

```
转手历史（3 次）：
1. Dalu 创建（2026-07-10）
2. 李明 修改 report.md、新增 conclusion.md（2026-07-14）
   说明："更新了Q3数据引用"
3. 王芳 修改 cleanup.py（2026-07-17）
```

### 6.3 信任状态

```typescript
import { getTrustRecord, setTrusted } from "../../features/identity/trust";

const fp = /* 从签名公钥计算指纹 */;
const record = getTrustRecord(fp, sigResult.originalAuthor ?? "Unknown");

// 显示
// record.trusted ? "已信任（12次）" : "首次见此签名者"
// 按钮 [加为信任联系人] / [移除信任]
```

---

## 七、信任列表（`features/identity/trust.ts`）

新建文件：

```typescript
const TRUST_KEY = "card-space-trusted-keys";

interface TrustRecord {
  fingerprint: string; label: string;
  firstSeen: number; count: number; trusted: boolean;
}

// 读取/递增某指纹的记录
export function getTrustRecord(fp: string, label: string): TrustRecord

// 设为信任/取消信任
export function setTrusted(fp: string, trusted: boolean): void

// 获取所有已信任指纹
export function getTrustedFingerprints(): Set<string>
```

存在 `localStorage`。

---

## 八、文件清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 重写 | `ui/panels/ExportPanel.tsx` | 从空壳→完整打包面板（元信息、文件选择、README、隐私扫描、加密/签名、修改说明、edits） |
| 修改 | `ui/card/CardView.tsx` | 右键菜单 + `onExportCard` prop |
| 修改 | `ui/canvas/Canvas.tsx` | `exportingCardIds` state + 多选导出 + 渲染 ExportPanel |
| 修改 | `features/export/index.ts` | 加 `EditRecord`/`EditsLog` 类型、per-file hash、`edits.json` 生成 |
| 修改 | `features/identity/index.ts` | 加 `hashSingleFile`、`exportIdentityKey`、`importIdentityKey` |
| 新建 | `features/identity/trust.ts` | 联系人信任列表 |
| 修改 | `ui/panels/SettingsPanel.tsx` | 加身份导出/导入区域 |
| 修改 | `ui/dialogs/ImportDialog.tsx` | 加 per-file hash 校验、edits.json 展示、信任状态 |

---

## 九、验收

1. `npx tsc --noEmit` 零错误
2. 右键单张卡 → "导出文件" → 浏览器下载原始文件
3. 右键单张卡 → "打包为 .card" → 面板打开 → 填信息 → 下载
4. 框选 3 张 → 右键 → "打包选中 3 张" → 面板列出所有文件 → 下载
5. 生成 README → 编辑区出现 → 包含在下载的 .card 里
6. 勾选加密 → 输入密码 → 导入时要求密码
7. 隐私扫描检测到 API Key → 面板底部显示警告
8. manifest.json 包含 `files: {"data.csv": "sha256:...", ...}`
9. 修改后重新打包 → edits.json 正确记录修改
10. 导入有 edits 的 .card → 面板展示转手历史
11. 导入面板校验每文件 hash → 被改文件高亮
12. 设置面板导出密钥 → 下载 JSON → 新浏览器导入 → 指纹一致
13. 首次导入某签名者 → "首次见" → 加信任 → 下次"已信任（2次）"
