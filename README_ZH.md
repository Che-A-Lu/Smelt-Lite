# Smelt Lite（炼知·轻）—— 起点

> 一个静态网页。拖文件进来变卡片。勾选卡片下载 `.card`。拖 `.card` 回来看到里面。三十秒。不需要安装、注册、AI 模型。

---

## 立刻用

已经在跟 AI 协作做东西？

1. **[复制提示词 →](prompts/)** 粘贴到你的 AI 对话里。AI 输出结构化 JSON。
2. **[打开 Smelt Lite →](https://che-a-lu.github.io/Smelt-Lite/)** 拖入 JSON。加素材。点打包。
3. **下载 `.card`。** 发给别人。他拖进去就看到全部。

不需要安装。不需要账号。不需要终端。

---

## 这是什么

**Smelt Lite** 是 Smelt 的第一个、最小的形态。它只做一件事：证明 `.card` 格式能跑通。

- 拖任何文件进来 → 生成卡片预览
- 勾选卡片 → 填个名字 → 下载 `.card`
- 拖 `.card` 进来 → 解包 → 看到里面有什么

就这些。没有画布。没有 AI。没有工作台。约 13 个源文件，TypeScript 零错误。

## .card 是什么

一张 `.card` 文件就是一个标准 ZIP 包。里面有：原始文件、完整的协作过程（每句 prompt、每次工具调用、AI 的思考链）、编辑记录、加密签名。改名 `.zip` 就能解压看到里面。

格式开放（MPL 2.0）。任何语言都能读写。不锁在任何平台。

## 为什么存在

做完一篇跟 AI 协作的论文之后，我发现除了 `.docx` 什么都没有——思考过程没了、推翻的路径没了、中间产物没了。我付出了时间和判断力，但带不走这个过程。

所有人都在做更好的 AI 工具。没有人在做「你做完之后带走的东西」。

**Smelt 不跟 AI 工具竞争。Smelt 收割你的对话——从任何 AI 工具——转成你拥有的 `.card` 文件。**

## 定义者，不是写代码的人

这个项目由一个跨专业大学生完成。没有手写一行代码——所有文件都是和 AI 对话产生的。但设计决策、交互规范、格式定义、质量标准是人的。作者可以讲出每一行代码为什么在那里，因为思考本身就是工作。

上一版 Smelt 膨胀到 47 个文件后被砍回了核心。这是谦卑的重启。

## 这不是什么

- 不是 AI 工作台
- 不是平台
- 不是 Chrome 扩展（还不是）
- 不是桌面应用（还不是）

## 下一步

Smelt 一步一步长，每个 `smelt-XX` 独立可用：

- **Smelt-Lite** ← 你在这里。静态网页，定义和验证 `.card`
- **Smelt-Ext** — Chrome 扩展。ChatGPT/Claude/Kimi 页面上一个按钮，一键导出 `.card`
- **Smelt-Desk** — 桌面应用。`.card` 成为原生文件类型，双击打开

每一步都独立可用。不等下一步。

## 试试

**在线使用：** [che-a-lu.github.io/Smelt-Lite](https://che-a-lu.github.io/Smelt-Lite/)

**本地运行：**
```bash
npm install
npm run dev        # → http://localhost:5173
npx tsc --noEmit   # 零错误
```

## 作者

[@Che-A-Lu](https://github.com/Che-A-Lu)，2026 年 7 月，上海。
